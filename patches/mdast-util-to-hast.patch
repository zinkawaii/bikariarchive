diff --git a/lib/footer.js b/lib/footer.js
index 3ccf095..fe3c6ff 100644
--- a/lib/footer.js
+++ b/lib/footer.js
@@ -126,7 +126,7 @@ export function footer(state) {
   const clobberPrefix =
     typeof state.options.clobberPrefix === 'string'
       ? state.options.clobberPrefix
-      : 'user-content-'
+      : 'footnote-'
   const footnoteBackContent =
     state.options.footnoteBackContent || defaultFootnoteBackContent
   const footnoteBackLabel =
@@ -179,15 +179,14 @@ export function footer(state) {
           href:
             '#' +
             clobberPrefix +
-            'fnref-' +
+            'ref-' +
             safeId +
             (rereferenceIndex > 1 ? '-' + rereferenceIndex : ''),
-          dataFootnoteBackref: '',
           ariaLabel:
             typeof footnoteBackLabel === 'string'
               ? footnoteBackLabel
               : footnoteBackLabel(referenceIndex, rereferenceIndex),
-          className: ['data-footnote-backref']
+          className: ['footnote-backref']
         },
         children: Array.isArray(children) ? children : [children]
       })
@@ -212,7 +211,7 @@ export function footer(state) {
     const listItem = {
       type: 'element',
       tagName: 'li',
-      properties: {id: clobberPrefix + 'fn-' + safeId},
+      properties: {id: clobberPrefix + safeId},
       children: state.wrap(content, true)
     }
 
@@ -228,7 +227,7 @@ export function footer(state) {
   return {
     type: 'element',
     tagName: 'section',
-    properties: {dataFootnotes: true, className: ['footnotes']},
+    properties: {className: ['footnotes']},
     children: [
       {
         type: 'element',
diff --git a/lib/handlers/footnote-reference.js b/lib/handlers/footnote-reference.js
index 7c65fa8..10253dc 100644
--- a/lib/handlers/footnote-reference.js
+++ b/lib/handlers/footnote-reference.js
@@ -20,7 +20,7 @@ export function footnoteReference(state, node) {
   const clobberPrefix =
     typeof state.options.clobberPrefix === 'string'
       ? state.options.clobberPrefix
-      : 'user-content-'
+      : 'footnote-'
   const id = String(node.identifier).toUpperCase()
   const safeId = normalizeUri(id.toLowerCase())
   const index = state.footnoteOrder.indexOf(id)
@@ -45,14 +45,14 @@ export function footnoteReference(state, node) {
     type: 'element',
     tagName: 'a',
     properties: {
-      href: '#' + clobberPrefix + 'fn-' + safeId,
+      href: '#' + clobberPrefix + safeId,
       id:
         clobberPrefix +
-        'fnref-' +
+        'ref-' +
         safeId +
         (reuseCounter > 1 ? '-' + reuseCounter : ''),
-      dataFootnoteRef: true,
-      ariaDescribedBy: ['footnote-label']
+      ariaDescribedBy: ['footnote-label'],
+      className: ['footnote-ref']
     },
     children: [{type: 'text', value: String(counter)}]
   }
